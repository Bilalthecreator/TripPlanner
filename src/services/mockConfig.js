/** Artificial network latency for mock services (ms). */
export const MOCK_DELAY_MS = 700

/** Chance (0–1) that an independent section fails, for testing error UI. Set 0 in normal use. */
export const MOCK_ERROR_RATE = 0
